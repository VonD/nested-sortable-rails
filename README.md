Rails 3.1 asset-pipeline gem to provide [nestedSortable](https://github.com/mjsarfatti/nestedSortable/tree/2.0alpha).

# Setup

In your Gemfile:

    gem 'nested-sortable-rails'

In your application.js manifest:

    //= require nested-sortable
